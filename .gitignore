.mcp.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local dev database (SQLite backend)
instance/
*.sqlite3

# Packaging output (PyInstaller / Inno Setup) and runtime data folder
build/
dist/
datos/
# Real configuration must never be committed (contains DB credentials).
registro-horario.env
